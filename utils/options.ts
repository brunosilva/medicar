// import { ISpecialties } from "../pages/new/hooks/type"

// export const formatResponse: (data: ISpecialties[]) => {
//   return (
//     (data: ISpecialties[])?.map((item: ISpecialties) => ({
//       text: item.nome,

//     }))
//   )
// };

// // export const formatResponse: (response: any) => {
// //   return (
// //     response?.map((item) => ({
// //       text: item?.nome,
// //       value: item?.id,
// //       key: item?.id,
// //     })) || []
// //   )
// // }